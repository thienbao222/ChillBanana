/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["images.unsplash.com", "m.media-amazon.com", "static.mercdn.net", "thumbnail.image.rakuten.co.jp"],
  },
};

export default nextConfig;
